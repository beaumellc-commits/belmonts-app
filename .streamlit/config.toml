[theme]
primaryColor = "#cc2020"
backgroundColor = "#f5f5f3"
secondaryBackgroundColor = "#ffffff"
textColor = "#0d1f38"
font = "sans serif"

[server]
headless = true
port = 8501
maxUploadSize = 1000          # upload max en MB (1 GB)
maxMessageSize = 1000         # taille max message WebSocket en MB

[browser]
gatherUsageStats = false
